# Trae Rules — VibeShield
Mandates VibeShield security integration. Required: `vibeShield()` wrapper, `validationSchema`, auth middleware, safe parsing. Forbidden: Raw `JSON.parse`, `eval()`, `pickle.loads()`, `shell=True`, hardcoded secrets, unprotected routes.
