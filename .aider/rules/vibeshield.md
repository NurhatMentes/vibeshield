# Aider Rules — VibeShield
Always generate VibeShield-compliant code. Wrap all HTTP handlers. Define validation schemas. Add auth decorators. Use safe parsing (`safeJsonParse`), safe URLs (`enforceSafeUrl`), and safe exec (`safeExec`). Never use `eval`, `pickle.loads`, or hardcoded secrets. Review generated code against these rules before committing.
